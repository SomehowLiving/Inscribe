# Secrets — never commit
.env
.env.*
!.env.example

# Vercel project linkage + pulled OIDC tokens
.vercel

# Dependencies
node_modules/

# Third-party reference clones (each has its own .git history)
repos/

# Capture/demo material — kept locally for recording, not committed
demo/

# OS / editor noise
.DS_Store
*.log
inscribe-extension.zip
